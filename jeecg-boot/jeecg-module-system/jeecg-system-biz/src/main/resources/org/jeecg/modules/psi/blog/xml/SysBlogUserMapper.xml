<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.psi.blog.mapper.SysBlogUserMapper">

    <resultMap id="BaseResultMap" type="org.jeecg.modules.psi.blog.entity.SysBlogUser">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="nickname" property="nickname" />
        <result column="avatar" property="avatar" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="bio" property="bio" />
        <result column="status" property="status" />
        <result column="del_flag" property="delFlag" />
        <result column="create_by" property="createBy" />
        <result column="create_time" property="createTime" />
        <result column="update_by" property="updateBy" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM sys_blog_user
        WHERE username = #{username}
        AND del_flag = '0'
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_blog_user
        WHERE id = #{id}
        AND del_flag = '0'
    </select>

    <select id="countArticlesByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM psi_blog_article
        WHERE author_id = #{userId}
        AND del_flag = '0'
    </select>

    <select id="sumLikesByUserId" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(like_count), 0) FROM psi_blog_article
        WHERE author_id = #{userId}
        AND del_flag = '0'
    </select>

</mapper>
